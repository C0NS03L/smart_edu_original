<!-- ... existing code ... -->
<nav class="mt-5 flex-1 space-y-1 px-2">
  <!-- ... existing navigation items ... -->

  <!-- Payment History navigation item -->
  <%= link_to payment_history_path,
              class:
                "group flex items-center rounded-md px-2 py-2 text-sm font-medium #{current_page?(payment_history_path) ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'}" do %>
    <svg
      xmlns="http://www.w3.org/2000/svg"
      class="#{current_page?(payment_history_path) ? 'text-gray-300' : 'text-gray-400 group-hover:text-gray-300'} mr-3 h-6 w-6 flex-shrink-0"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"
      />
    </svg>
    Payment History
  <% end %>

  <!-- ... existing navigation items ... -->
</nav>
<!-- ... existing code ... -->

<% content_for :title, 'Code Generation' %>
<div class="flex min-h-[calc(100vh-64px)] flex-col items-center justify-center space-y-8">
  <% if flash[:notice] %>
    <div class="alert alert-success"><%= flash[:notice] %></div>
  <% elsif flash[:alert] %>
    <div class="alert alert-danger"><%= flash[:alert] %></div>
  <% end %>
  <div class="text-center">
    <div class="rounded p-4 shadow-lg">
      <h2 class="mb-4 text-3xl font-bold">Staff Code</h2>
      <%= form_with url: generate_staff_code_principal_path, method: :post, local: true do |form| %>
        <label for="staff_uses" class="mb-2 block text-lg font-medium">Number of Accounts Required</label>
        <%= form.number_field :usage_limit, id: 'staff_uses', class: 'input input-bordered mb-4 w-full', placeholder: 'Enter Number' %>
        <%= form.submit 'Generate', class: 'btn btn-primary w-full' %>
      <% end %>
    </div>
  </div>
  <div class="text-center">
    <div class="rounded p-4 shadow-lg">
      <h2 class="mb-4 text-3xl font-bold">Student Code</h2>
      <%= form_with url: generate_student_code_principal_path, method: :post, local: true do |form| %>
        <label for="student_uses" class="mb-2 block text-lg font-medium">Number of Accounts Required</label>
        <%= form.number_field :usage_limit,
                              id: 'student_uses',
                              class: 'input input-bordered mb-4 w-full',
                              placeholder: 'Enter Number' %>
        <%= form.submit 'Generate', class: 'btn btn-primary w-full' %>
      <% end %>
    </div>
  </div>
  <div class="text-center"><%= link_to 'Back', principal_dashboard_path, class: 'btn btn-secondary w-full' %></div>
</div>
